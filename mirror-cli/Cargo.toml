[package]
name = "mirror-cli"
version = "0.1.0"
edition = "2021"
description = "Command-line interface for managing mirror.toml configuration files"
authors = ["MirrorBoards Team"]
license = "MIT"

[dependencies]
mirror-sdk = { path = "../mirror-sdk" }
clap = { version = "4.4", features = ["derive"] }
colored = "2.0"
thiserror = "1.0"
dirs = "5.0"
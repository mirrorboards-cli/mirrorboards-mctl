[workspace]
members = [
    "mirror-sdk",
    "mirror-cli",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["MirrorBoards Team"]
license = "MIT"
repository = "https://github.com/mirrorboards/mirror-workspace"

# Dependencies for workspace examples
[dev-dependencies]
tempfile = "3.8"

# Examples at the workspace level
[[example]]
name = "sdk_and_cli"
path = "examples/sdk_and_cli.rs"